version: "3.8"

services:
  phynai:
    build: .
    container_name: phynai-agent
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - phynai-data:/opt/data
    env_file:
      - .env
    environment:
      - PHYNAI_HOME=/opt/data

volumes:
  phynai-data:
